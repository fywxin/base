package ${domain.pkgName!"com.cyou.fz.trade"}.${domain.domainName?uncap_first}.service;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import com.cyou.fz.commons.mybatis.selecterplus.mybatis.service.BaseService;
import ${domain.pkgName!"org.whale.system"}.${domain.domainName?uncap_first}.bean.${domain.domainName};

/**
* Created by wjs on ${.now?date}.
*/
@Service
public class ${domain.domainName}Service extends BaseService<${domain.domainName}> {

}